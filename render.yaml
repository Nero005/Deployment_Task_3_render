services:
  - type: web
    name: render-guestbook
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: guestbook-db
          property: connectionString

databases:
  - name: guestbook-db
    plan: free